function fetch(e){return new Promise((t,a)=>{https.get({hostname:"hemu0626.github.io",path:e,headers:{"User-Agent":"Mozilla/5.0"}},e=>{let a="";e.on("data",e=>a+=e),e.on("end",()=>t(a))}).on("error",a)})}const https=require("https");(async()=>{const e=await fetch("/"),t=e.indexOf("<body"),a=e.indexOf("<main",t),c=(e.slice(a>0?a:t,(a>0?a:t)+1200).replace(/\s+/g," "),[...e.matchAll(/href="([^"]+\.css[^"]*)"/g)].map(e=>e[1]));for(const e of c){const t=await fetch(e),a=[...t.matchAll(/[^{}]*max-width[^{}]*\{[^}]*\}/g)].map(e=>e[0].slice(0,200)),c=[...t.matchAll(/[^{}]*930[^{}]*\{[^}]*\}/g)].map(e=>e[0].slice(0,200));[...t.matchAll(/[^{}]*container[^{}]*\{[^}]*\}/g)].map(e=>e[0].slice(0,200)).slice(0,12);a.slice(0,12).forEach(()=>{}),c.length&&c.slice(0,6).forEach(()=>{})}})();